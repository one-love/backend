language: python

python:
  - 2.7

install:
  - pip install -r requirements_common.txt
  - pip install -r requirements_test.txt
  - cp project/settings/__init__.py.template project/settings/__init__.py

script:
  - coverage run --source=onelove manage.py test

after_script:
  - coveralls
